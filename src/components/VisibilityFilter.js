/**
 * Visibility filter
 * 
 * Constant file to store visibility filter as object variable (enum like)
 */
const FILTER = {
  ALL: "ALL",
  COMPLETED: "COMPLETED",
  PENDING: "PENDING"
};

export default FILTER;
